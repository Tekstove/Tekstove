{% extends 'TekstoveBundle::main.html.twig' %}

{% block content %}
    {% if error %}
        {{ error|e }}
    {% endif %}
    
    {{ form_start(form) }}
        {{ form_row(form.username) }}
        {{ form_row(form.password) }}
        {{ form_row(form.recaptcha) }}
    {{ form_end(form) }}
{% endblock %}