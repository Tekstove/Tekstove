{% extends 'SiteBundle::main.html.twig' %}
{% block content %}
    <h1>
        {{ album.name }}
        {% if album.year is not empty %}
            ({{ album.year }})
        {% endif %}
    </h1>
    
    {% if (album.acl('edit')) %}
        <a class="btn btn-warning" href="#">Промени</a>
    {% endif %}
        
    <img src="{{ album.image }}">
{% endblock %}
