<div class="panel panel-default">
    <div class="panel-body">
        <a href="{{ path('userPmList') }}">
            {% if app.user.getUnreadPmCount > 0 %}
                Ново ЛС <span class="badge">{{ app.user.getUnreadPmCount }}</span>
            {% else %}
                Лични съобщения
            {% endif %}
        </a>
        
        <br/>
        
        <a href="{{ path('userView', {id: app.user.id}) }}">Профил</a>

        {#
        <i>Моето&nbsp;Меню:</i><br/>
        Ранг:&nbsp;
        {{ app.user.className|e }}
        Превод Заявки: <b>630</b>
        Проблеми <b>4598
        Слушай <b>любими</b>
        Мои преводи
        Изтрити песни (всички)
        #}
    </div>
</div>
