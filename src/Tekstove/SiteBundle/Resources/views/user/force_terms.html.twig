{% extends '@Site/main.html.twig' %}
{% block content %}
    <h1>
        {{ user.username|e }}
    </h1>

    {{ form_start(form) }}

    Правила
    <hr>
    {% include '@Site/static/terms-text.html.twig' %}
    <hr>

    
    {{ form_row(form.termsAccepted) }}

    {{ form_end(form) }}

    <a href="{{ path('tekstove.site.user.delete', { id: user.id }) }}" class="btn btn-danger">Не съм съгласен с условията</a>
{% endblock %}
