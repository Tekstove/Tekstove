{% extends 'SiteBundle::main.html.twig' %}

{% block title %}
    Най-популярни песни {{ monthName|trans }} {{year}}
{% endblock %}

{% block content %}

    <h1>
        Най-популярни песни {{ monthName|trans }} {{year}}
    </h1>

    <table class="table">
        <tr>
            <thead>
                <th>
                    №
                </th>
                <th>
                    Песен
                </th>
                <th>
                    Популярност
                </th>
            </thead>
        </tr>
        {% for lyricHistory in lyricsHistory %}
            <tr>
                <td>
                    {{ loop.index }}
                </td>
                <td>
                    <a href="{{ path('lyricView', { id: lyricHistory.lyric.id }) }}">
                        {{ lyricHistory.lyric.cacheTitleShort }}
                    </a>
                </td>
                <td>
                    {{ lyricHistory.popularity }}
                </td>
            </tr>
        {% endfor %}
    </table>

    <nav>
        <ul class="pagination">
            <li
                {% if prevMonthLink is empty %}
                    class="disabled"
                {% endif %}
            >
                <a href="{{ prevMonthLink }}">
                    « Предишен месец
                </a>
            </li>

            <li
                {% if nextMonthLink is empty %}
                    class="disabled"
                {% endif %}
            >
                <a href="{{ nextMonthLink }}">
                    Следващ месец »
                </a>
            </li>
        </ul>
    </nav>
{% endblock %}
