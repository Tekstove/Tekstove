{% extends 'SiteBundle::main.html.twig' %}
{% block content %}
    {{ form_start(form) }}

    {{ form_row(form.artists) }}
    
    {{ form_row(form.title) }}
    {{ form_row(form.text) }}
    {{ form_row(form.textBg) }}

    {{ form_rest(form) }}
    
    {{ form_end(form) }}
{% endblock %}
