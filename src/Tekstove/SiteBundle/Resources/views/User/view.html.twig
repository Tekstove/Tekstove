{% extends 'SiteBundle::main.html.twig' %}
{% block content %}

    <h1>
        {{ user.username|e }}
    </h1>
    
    {% if (user.avatar) %}
        <br/>
        <br/>
        <img src="{{ user.avatar|e }}" />
    {% endif %}
    
    
    <a href="{{ path('tekstove_site.user.pm.send', {toUserId: user.id}) }}">
        <img src="{{ asset('bundles/site/images/icons/mail/mail.png') }}"  style="width: 20px;"/>
        изпрати ЛС
    </a>
    
    <br/>
    <br/>
    {{ user.about|e|nl2br }}
    
{#    {% if user.roles %}
        <br/>
        Групи:
        {% for role in user.roles %}
            <img 
                src="{{ asset(role.image) }}" 
                alt="{{ role.name|e }}"
                title="{{ role.name|e }}"
            />
        {% endfor %}
    {% endif %}#}
     
{% endblock %}
