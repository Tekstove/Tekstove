{% extends 'SiteBundle::main.html.twig' %}
{% block content %}
    <h1>
        {{ user.username|e }}
    </h1>

    {{ form_start(form) }}


    {{ form_end(form) }}

    <a href="{{ path('tekstove.site.user.forceTerms', {id: user.id}) }}">Правила - преглед, съгласие и отказ от съгласие</a>
{% endblock %}
